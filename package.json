{
  "name": "daily-report-app",
  "version": "2.0.0",
  "description": "Daily report web app with shared-folder sync and optional Vercel Postgres cloud backup.",
  "author": "Daily Report App",
  "private": false,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check app.js && node --check api/app-auth.js && node --check api/cloud-backup.js && node --check api/cloud-data.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  }
}
